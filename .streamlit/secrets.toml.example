# Copy to secrets.toml and fill in. secrets.toml is gitignored - never commit it.
# Streamlit reads this via st.secrets[...]. On Streamlit Community Cloud,
# paste these under Settings -> Secrets instead of shipping a file.

[twitter]
consumer_key        = ""
consumer_secret     = ""
access_token        = ""
access_token_secret = ""
